{
  "name": "boring-pushowl-webhook",
  "version": "1.0.0",
  "description": "Cloudflare Worker that tags Shopify customers as members when they subscribe via PushOwl.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5",
    "wrangler": "^3"
  }
}
